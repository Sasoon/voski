---
import {
  instrumental,
  musicianship,
  ensemble,
  groupIntroClass,
  pianoAccompaniment,
  groupLearningSessions,
} from './FaqData.js';
---

<section class="py-16 sm:py-20">
  <div class="mx-auto max-w-2xl px-4 sm:px-6 lg:max-w-7xl lg:px-8">
    <div class="mx-auto grid gap-12 text-center lg:gap-8">
      <h1 class="text-4xl font-medium tracking-tight sm:text-5xl lg:text-6xl">
        Frequently Asked Questions
      </h1>
      <div class="faq-content rounded-sm border border-primary-900/10">
        <!-- Instrumental Collapsible Heading -->
        <div
          class="mx-auto w-11/12 border-b border-primary-900/10"
          x-data="{ isOpenInstrumental: false }"
        >
          <h2
            class="cursor-pointer text-3xl font-medium tracking-tight sm:text-4xl"
            @click="isOpenInstrumental = !isOpenInstrumental"
          >
            Instrumental
          </h2>
          <!-- FAQ Section - Only shows when Instrumental is expanded -->
          <div x-show="isOpenInstrumental" x-cloak x-collapse>
            <dl
              class="-mt-3"
              x-data="{ selected: null, toggle(value) { this.selected = this.selected === value ? null : value } }"
            >
              {
                instrumental.map((question, index) => (
                  <div
                    class="rounded-3xl px-4 transition"
                    x-data={`{ id: ${index}, get isOpen() { return this.id === this.selected }, get isNextOpen() { return (this.id + 1) === this.selected } }`}
                    x-bind:class="{ 'bg-primary-500/10 dark:bg-primary-400/10': isOpen }"
                  >
                    <dt
                      class="border-b text-lg transition"
                      x-cloak
                      x-bind:class="{ 'border-transparent': isOpen || isNextOpen, 'border-primary-900/10 dark:border-primary-300/10': !(isOpen || isNextOpen) }"
                    >
                      <button
                        type="button"
                        class="group block w-full py-6 text-left transition focus-visible:outline-none"
                        aria-controls={question.id}
                        x-on:click="toggle(id)"
                        aria-expanded="false"
                        x-bind:aria-expanded="isOpen.toString()"
                      >
                        <div class="flex items-center justify-between rounded-3xl group-focus-visible:outline group-focus-visible:outline-2 group-focus-visible:outline-offset-2 group-focus-visible:outline-primary-950 dark:group-focus-visible:outline-primary-200">
                          <span class="font-medium">{question.title}</span>
                          <span class="ml-6 flex h-7 items-center">
                            <svg
                              class="h-6 w-6 rotate-0 transform text-primary-600 transition duration-200 ease-in-out dark:text-primary-400"
                              x-bind:class="{ '-rotate-180': isOpen, 'rotate-0': !isOpen }"
                              xmlns="http://www.w3.org/2000/svg"
                              viewBox="0 0 24 24"
                              fill="currentColor"
                              aria-hidden="true"
                            >
                              <path d="M11.9997 13.1714L16.9495 8.22168L18.3637 9.63589L11.9997 15.9999L5.63574 9.63589L7.04996 8.22168L11.9997 13.1714Z" />
                            </svg>
                          </span>
                        </div>
                      </button>
                    </dt>
                    <dd class="m-4 pb-6 pr-6" id={question.id} x-show="isOpen" x-cloak x-collapse>
                      <p class="text-base text-primary-950/70 dark:text-primary-200/70">
                        {question.answer}
                      </p>
                    </dd>
                  </div>
                ))
              }
            </dl>
          </div>
        </div>
        <!-- Musicianship Collapsible Heading -->
        <div
          class="mx-auto w-11/12 border-b border-primary-900/10"
          x-data="{ isOpenMusicianship: false }"
        >
          <h2
            class="cursor-pointer text-3xl font-medium tracking-tight sm:text-4xl"
            @click="isOpenMusicianship = !isOpenMusicianship"
          >
            Musicianship
          </h2>
          <!-- FAQ Section - Only shows when Musicianship is expanded -->
          <div x-show="isOpenMusicianship" x-cloak x-collapse>
            <dl
              class="-mt-3"
              x-data="{ selected: null, toggle(value) { this.selected = this.selected === value ? null : value } }"
            >
              {
                musicianship.map((question, index) => (
                  <div
                    class="rounded-3xl px-4 transition"
                    x-data={`{ id: ${index}, get isOpen() { return this.id === this.selected }, get isNextOpen() { return (this.id + 1) === this.selected } }`}
                    x-bind:class="{ 'bg-primary-500/10 dark:bg-primary-400/10': isOpen }"
                  >
                    <dt
                      class="border-b text-lg transition"
                      x-cloak
                      x-bind:class="{ 'border-transparent': isOpen || isNextOpen, 'border-primary-900/10 dark:border-primary-300/10': !(isOpen || isNextOpen) }"
                    >
                      <button
                        type="button"
                        class="group block w-full py-6 text-left transition focus-visible:outline-none"
                        aria-controls={question.id}
                        x-on:click="toggle(id)"
                        aria-expanded="false"
                        x-bind:aria-expanded="isOpen.toString()"
                      >
                        <div class="flex items-center justify-between rounded-3xl group-focus-visible:outline group-focus-visible:outline-2 group-focus-visible:outline-offset-2 group-focus-visible:outline-primary-950 dark:group-focus-visible:outline-primary-200">
                          <span class="font-medium">{question.title}</span>
                          <span class="ml-6 flex h-7 items-center">
                            <svg
                              class="h-6 w-6 rotate-0 transform text-primary-600 transition duration-200 ease-in-out dark:text-primary-400"
                              x-bind:class="{ '-rotate-180': isOpen, 'rotate-0': !isOpen }"
                              xmlns="http://www.w3.org/2000/svg"
                              viewBox="0 0 24 24"
                              fill="currentColor"
                              aria-hidden="true"
                            >
                              <path d="M11.9997 13.1714L16.9495 8.22168L18.3637 9.63589L11.9997 15.9999L5.63574 9.63589L7.04996 8.22168L11.9997 13.1714Z" />
                            </svg>
                          </span>
                        </div>
                      </button>
                    </dt>
                    <dd class="m-4 pb-6 pr-6" id={question.id} x-show="isOpen" x-cloak x-collapse>
                      <p class="text-base text-primary-950/70 dark:text-primary-200/70">
                        {question.answer}
                      </p>
                    </dd>
                  </div>
                ))
              }
            </dl>
          </div>
        </div>
        <!-- Group Introductory Class Collapsible Heading -->
        <div
          class="mx-auto w-11/12 border-b border-primary-900/10"
          x-data="{ isOpenGroupIntroClass: false }"
        >
          <h2
            class="cursor-pointer text-3xl font-medium tracking-tight sm:text-4xl"
            @click="isOpenGroupIntroClass = !isOpenGroupIntroClass"
          >
            Group Introductory Class
          </h2>
          <!-- FAQ Section - Only shows when GroupIntroClass is expanded -->
          <div x-show="isOpenGroupIntroClass" x-cloak x-collapse>
            <dl
              class="-mt-3"
              x-data="{ selected: null, toggle(value) { this.selected = this.selected === value ? null : value } }"
            >
              {
                groupIntroClass.map((question, index) => (
                  <div
                    class="rounded-3xl px-4 transition"
                    x-data={`{ id: ${index}, get isOpen() { return this.id === this.selected }, get isNextOpen() { return (this.id + 1) === this.selected } }`}
                    x-bind:class="{ 'bg-primary-500/10 dark:bg-primary-400/10': isOpen }"
                  >
                    <dt
                      class="border-b text-lg transition"
                      x-cloak
                      x-bind:class="{ 'border-transparent': isOpen || isNextOpen, 'border-primary-900/10 dark:border-primary-300/10': !(isOpen || isNextOpen) }"
                    >
                      <button
                        type="button"
                        class="group block w-full py-6 text-left transition focus-visible:outline-none"
                        aria-controls={question.id}
                        x-on:click="toggle(id)"
                        aria-expanded="false"
                        x-bind:aria-expanded="isOpen.toString()"
                      >
                        <div class="flex items-center justify-between rounded-3xl group-focus-visible:outline group-focus-visible:outline-2 group-focus-visible:outline-offset-2 group-focus-visible:outline-primary-950 dark:group-focus-visible:outline-primary-200">
                          <span class="font-medium">{question.title}</span>
                          <span class="ml-6 flex h-7 items-center">
                            <svg
                              class="h-6 w-6 rotate-0 transform text-primary-600 transition duration-200 ease-in-out dark:text-primary-400"
                              x-bind:class="{ '-rotate-180': isOpen, 'rotate-0': !isOpen }"
                              xmlns="http://www.w3.org/2000/svg"
                              viewBox="0 0 24 24"
                              fill="currentColor"
                              aria-hidden="true"
                            >
                              <path d="M11.9997 13.1714L16.9495 8.22168L18.3637 9.63589L11.9997 15.9999L5.63574 9.63589L7.04996 8.22168L11.9997 13.1714Z" />
                            </svg>
                          </span>
                        </div>
                      </button>
                    </dt>
                    <dd class="m-4 pb-6 pr-6" id={question.id} x-show="isOpen" x-cloak x-collapse>
                      <p class="text-base text-primary-950/70 dark:text-primary-200/70">
                        {question.answer}
                      </p>
                    </dd>
                  </div>
                ))
              }
            </dl>
          </div>
        </div>
        <!-- Piano Accompaniment Collapsible Heading -->
        <div
          class="mx-auto w-11/12 border-b border-primary-900/10"
          x-data="{ isOpenPianoAccompaniment: false }"
        >
          <h2
            class="cursor-pointer text-3xl font-medium tracking-tight sm:text-4xl"
            @click="isOpenPianoAccompaniment = !isOpenPianoAccompaniment"
          >
            Piano Accompaniment
          </h2>
          <!-- FAQ Section - Only shows when PianoAccompaniment is expanded -->
          <div x-show="isOpenPianoAccompaniment" x-cloak x-collapse>
            <dl
              class="-mt-3"
              x-data="{ selected: null, toggle(value) { this.selected = this.selected === value ? null : value } }"
            >
              {
                pianoAccompaniment.map((question, index) => (
                  <div
                    class="rounded-3xl px-4 transition"
                    x-data={`{ id: ${index}, get isOpen() { return this.id === this.selected }, get isNextOpen() { return (this.id + 1) === this.selected } }`}
                    x-bind:class="{ 'bg-primary-500/10 dark:bg-primary-400/10': isOpen }"
                  >
                    <dt
                      class="border-b text-lg transition"
                      x-cloak
                      x-bind:class="{ 'border-transparent': isOpen || isNextOpen, 'border-primary-900/10 dark:border-primary-300/10': !(isOpen || isNextOpen) }"
                    >
                      <button
                        type="button"
                        class="group block w-full py-6 text-left transition focus-visible:outline-none"
                        aria-controls={question.id}
                        x-on:click="toggle(id)"
                        aria-expanded="false"
                        x-bind:aria-expanded="isOpen.toString()"
                      >
                        <div class="flex items-center justify-between rounded-3xl group-focus-visible:outline group-focus-visible:outline-2 group-focus-visible:outline-offset-2 group-focus-visible:outline-primary-950 dark:group-focus-visible:outline-primary-200">
                          <span class="font-medium">{question.title}</span>
                          <span class="ml-6 flex h-7 items-center">
                            <svg
                              class="h-6 w-6 rotate-0 transform text-primary-600 transition duration-200 ease-in-out dark:text-primary-400"
                              x-bind:class="{ '-rotate-180': isOpen, 'rotate-0': !isOpen }"
                              xmlns="http://www.w3.org/2000/svg"
                              viewBox="0 0 24 24"
                              fill="currentColor"
                              aria-hidden="true"
                            >
                              <path d="M11.9997 13.1714L16.9495 8.22168L18.3637 9.63589L11.9997 15.9999L5.63574 9.63589L7.04996 8.22168L11.9997 13.1714Z" />
                            </svg>
                          </span>
                        </div>
                      </button>
                    </dt>
                    <dd class="m-4 pb-6 pr-6" id={question.id} x-show="isOpen" x-cloak x-collapse>
                      <p class="text-base text-primary-950/70 dark:text-primary-200/70">
                        {question.answer}
                      </p>
                    </dd>
                  </div>
                ))
              }
            </dl>
          </div>
        </div>
        <!-- Group Learning Sessions Collapsible Heading -->
        <div
          class="mx-auto w-11/12 border-b border-primary-900/10"
          x-data="{ isOpenGroupLearningSessions: false }"
        >
          <h2
            class="cursor-pointer text-3xl font-medium tracking-tight sm:text-4xl"
            @click="isOpenGroupLearningSessions = !isOpenGroupLearningSessions"
          >
            Group Learning Sessions
          </h2>
          <!-- FAQ Section - Only shows when GroupLearningSessions is expanded -->
          <div x-show="isOpenGroupLearningSessions" x-cloak x-collapse>
            <dl
              class="-mt-3"
              x-data="{ selected: null, toggle(value) { this.selected = this.selected === value ? null : value } }"
            >
              {
                groupLearningSessions.map((question, index) => (
                  <div
                    class="rounded-3xl px-4 transition"
                    x-data={`{ id: ${index}, get isOpen() { return this.id === this.selected }, get isNextOpen() { return (this.id + 1) === this.selected } }`}
                    x-bind:class="{ 'bg-primary-500/10 dark:bg-primary-400/10': isOpen }"
                  >
                    <dt
                      class="border-b text-lg transition"
                      x-cloak
                      x-bind:class="{ 'border-transparent': isOpen || isNextOpen, 'border-primary-900/10 dark:border-primary-300/10': !(isOpen || isNextOpen) }"
                    >
                      <button
                        type="button"
                        class="group block w-full py-6 text-left transition focus-visible:outline-none"
                        aria-controls={question.id}
                        x-on:click="toggle(id)"
                        aria-expanded="false"
                        x-bind:aria-expanded="isOpen.toString()"
                      >
                        <div class="flex items-center justify-between rounded-3xl group-focus-visible:outline group-focus-visible:outline-2 group-focus-visible:outline-offset-2 group-focus-visible:outline-primary-950 dark:group-focus-visible:outline-primary-200">
                          <span class="font-medium">{question.title}</span>
                          <span class="ml-6 flex h-7 items-center">
                            <svg
                              class="h-6 w-6 rotate-0 transform text-primary-600 transition duration-200 ease-in-out dark:text-primary-400"
                              x-bind:class="{ '-rotate-180': isOpen, 'rotate-0': !isOpen }"
                              xmlns="http://www.w3.org/2000/svg"
                              viewBox="0 0 24 24"
                              fill="currentColor"
                              aria-hidden="true"
                            >
                              <path d="M11.9997 13.1714L16.9495 8.22168L18.3637 9.63589L11.9997 15.9999L5.63574 9.63589L7.04996 8.22168L11.9997 13.1714Z" />
                            </svg>
                          </span>
                        </div>
                      </button>
                    </dt>
                    <dd class="m-4 pb-6 pr-6" id={question.id} x-show="isOpen" x-cloak x-collapse>
                      <p class="text-base text-primary-950/70 dark:text-primary-200/70">
                        {question.answer}
                      </p>
                    </dd>
                  </div>
                ))
              }
            </dl>
          </div>
        </div>
        <!-- Ensemble Collapsible Heading -->
        <div class="mx-auto w-11/12" x-data="{ isOpenEnsemble: false }">
          <h2
            class="cursor-pointer text-3xl font-medium tracking-tight sm:text-4xl"
            @click="isOpenEnsemble = !isOpenEnsemble"
          >
            Ensemble
          </h2>
          <!-- FAQ Section - Only shows when Ensemble is expanded -->
          <div x-show="isOpenEnsemble" x-cloak x-collapse>
            <dl
              class="-mt-3"
              x-data="{ selected: null, toggle(value) { this.selected = this.selected === value ? null : value } }"
            >
              {
                ensemble.map((question, index) => (
                  <div
                    class="rounded-3xl px-4 transition"
                    x-data={`{ id: ${index}, get isOpen() { return this.id === this.selected }, get isNextOpen() { return (this.id + 1) === this.selected } }`}
                    x-bind:class="{ 'bg-primary-500/10 dark:bg-primary-400/10': isOpen }"
                  >
                    <dt
                      class="border-b text-lg transition"
                      x-cloak
                      x-bind:class="{ 'border-transparent': isOpen || isNextOpen, 'border-primary-900/10 dark:border-primary-300/10': !(isOpen || isNextOpen) }"
                    >
                      <button
                        type="button"
                        class="group block w-full py-6 text-left transition focus-visible:outline-none"
                        aria-controls={question.id}
                        x-on:click="toggle(id)"
                        aria-expanded="false"
                        x-bind:aria-expanded="isOpen.toString()"
                      >
                        <div class="flex items-center justify-between rounded-3xl group-focus-visible:outline group-focus-visible:outline-2 group-focus-visible:outline-offset-2 group-focus-visible:outline-primary-950 dark:group-focus-visible:outline-primary-200">
                          <span class="font-medium">{question.title}</span>
                          <span class="ml-6 flex h-7 items-center">
                            <svg
                              class="h-6 w-6 rotate-0 transform text-primary-600 transition duration-200 ease-in-out dark:text-primary-400"
                              x-bind:class="{ '-rotate-180': isOpen, 'rotate-0': !isOpen }"
                              xmlns="http://www.w3.org/2000/svg"
                              viewBox="0 0 24 24"
                              fill="currentColor"
                              aria-hidden="true"
                            >
                              <path d="M11.9997 13.1714L16.9495 8.22168L18.3637 9.63589L11.9997 15.9999L5.63574 9.63589L7.04996 8.22168L11.9997 13.1714Z" />
                            </svg>
                          </span>
                        </div>
                      </button>
                    </dt>
                    <dd class="pb-6 pr-6" id={question.id} x-show="isOpen" x-cloak x-collapse>
                      <p class="text-base text-primary-950/70 dark:text-primary-200/70">
                        {question.answer}
                      </p>
                    </dd>
                  </div>
                ))
              }
            </dl>

            <p class="text-gray-500 mt-6 text-base">
              Additional information about invoices, fees, lesson specifics...
            </p>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
